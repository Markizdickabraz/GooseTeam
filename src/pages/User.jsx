import { UserForm } from '../components/account/UserForm';
import { UserSection } from './User.styled';

const User = () => {
  return (
    <UserSection>
      <UserForm />
    </UserSection>
  );
};

export default User;
